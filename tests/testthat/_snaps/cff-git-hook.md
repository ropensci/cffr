# cff_git_hook_install errors when usethis is unavailable

    Code
      cff_git_hook_install()
    Condition
      Error in `cff_git_hook_install()`:
      ! Install usethis to use the pre-commit hook.
      i Run `install.packages("usethis")`.

