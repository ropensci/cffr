# cff_gha_update errors when workflow copy fails

    Code
      cff_gha_update(path = new_dir)
    Message
      i Creating directory '<package>/.github/workflows'.
    Condition
      Error in `cff_gha_update()`:
      ! Cannot install workflow at '<package>/.github/workflows/update-citation-cff.yaml'.
      i Check write permissions for '<package>/.github/workflows'.

